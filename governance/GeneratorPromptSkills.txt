SkillsFor: GeneratorPrompt
SkillsVersion: 10.1.0
Generated: 2026-06-09

Skills jsou konfigurovány per agent v Claude.ai.
Každý Generator agent musí mít aktivních ASPOŇ 10 skills.

!! MAINTENANCE: Při úpravě zkontrolovat konzistenci s CatalogPrompt/ManagerPrompt skills.

=============================================================
PRAVIDLA POUŽITÍ SKILLS
=============================================================

- pokud skill pokrývá úkol: použít skill, má přednost před obecným workflow
- pokud skill odporuje canonical governance: canonical governance má přednost
- nikdy netvrdit, že skill existuje pokud není dostupný
- uvést SkillsUsed[] v run reportu
- skill NENÍ v tomto Skill Directory: ZABLOKOVAT použití

=============================================================
SKILL DIRECTORY (24 skills)
=============================================================

-- A. DB & COVERAGE (3) ----------------------------------------

## bot-coverage-analyzer
Task types: coverage check, bot entity statistics, image coverage %
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Analyzuje pokrytí obrázků pro bot entity v USM.
  Spočítá: celkový počet bot týmů, počet bez loga, coverage %.
  Totéž pro bot hráče. Vrátí porovnání a doporučení priority.
Inputs: DB připojení, název tabulek (Teams, Players nebo ekvivalenty)
Outputs: teamsCoverage %, playersCoverage %, priority doporučení
Limitations: pracuje pouze s IsBot = true entitami. Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

## bot-entity-verifier
Task types: verify IsBot, image missing check, pre-generate validation
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Před každým generováním znovu ověří, že entita splňuje scope:
  IsBot = true a obrázek skutečně stále chybí (NULL nebo prázdný).
  Chrání před zápisem na nesprávnou entitu.
Inputs: EntityGuid nebo jiný identifikátor, tabulka
Outputs: verified/skip flag, skip reason pokud odmítnut
Limitations: přímý DB dotaz; nepoužívat cached data ze startup fáze. Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

## missing-image-batch-loader
Task types: načtení dávky entit bez obrázku, max 10
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Načte seznam max 10 bot entit bez obrázku (LogoUrl nebo PhotoUrl NULL/prázdný).
  Řadí dle data vytvoření (nejstarší první).
  Respektuje scope: IsBot = true only.
Inputs: tabulka, sloupec s URL obrázku, max batch size (default 10)
Outputs: seznam entit s jejich Guid a dostupnými atributy
Limitations: max 10 entit per volání; neřadí dle priority scoringu. Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

-- B. PROMPT BUILDING (4) --------------------------------------

## team-logo-prompt-builder
Task types: sestavení promptu pro týmové logo, BotProfile logic
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Sestaví image prompt pro týmové logo dle specifikace GeneratorPrompt Ch 5.
  Doplní Name, Code3, Stadium, BotProfile. Přidá sport-specific instrukce
  a BotProfile barvy. Zajistí "No text, no letters" instrukci.
Inputs: detail týmu (Name, Code3, Stadium, BotProfile, sport context)
Outputs: hotový prompt string pro create_image
Limitations: pokud chybí Name → odmítnout; ostatní atributy jsou volitelné. Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

## player-photo-prompt-builder
Task types: sestavení promptu pro fotku hráče, sport context
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Sestaví image prompt pro hráčský portrét dle specifikace GeneratorPrompt Ch 5.
  Doplní FullName, Position, BotArchetype. Přidá sport-specific instrukce.
  Zajistí "chest-up, one person, realistic" instrukci.
Inputs: detail hráče (FullName, Position, BotArchetype, sport context)
Outputs: hotový prompt string pro create_image
Limitations: pokud chybí FullName → odmítnout; ostatní atributy jsou volitelné. Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

## sport-context-resolver
Task types: detekce sportu z Position nebo BotArchetype
Description: Domain-specifický skill (USM). Přizpůsobit pro jiné domény dle config. Rozpozná sport entity z dostupných atributů (Position, BotArchetype, TeamName).
  Mapuje na sport-specific prompt rozšíření dle tabulky v Ch 5 GeneratorPrompt.
Inputs: Position (string), BotArchetype (string, volitelně), TeamName (string, volitelně)
Outputs: detekovaný sport, prompt extension string, confidence (high/medium/low)
Limitations: nejistá detekce → confidence=low, použít generic "sport background". Hardcoded pro USM bot entities — pro jiné domény vyžaduje adaptaci.

## prompt-refinement-handler
Task types: upřesnění promptu po quality check fail, retry
Description: Pokud vygenerovaný obrázek nesplňuje quality rules, upřesní prompt
  přidáním konkrétních instrukcí k problematickým prvkům.
  Loga s textem → přidat "Strictly no text, no letters, no numbers".
  Portrét s více osobami → přidat "Only one athlete, single person".
Inputs: původní prompt, důvod quality fail
Outputs: upřesněný prompt pro 1 retry
Limitations: max 1 refinement per entita; při druhém selhání → skip

-- C. GENERATION & QUALITY (3) ---------------------------------

## image-generator-orchestrator
Task types: create_image, square format, 1024×1024
Description: Orchestruje volání create_image. Zajistí vždy čtvercový formát 1:1,
  cílově 1024×1024. Předá prompt, zkontroluje že výsledek byl vrácen.
  [ChatGPT native — Claude agenti nemají tento nástroj. Na Claude platformě použít alternativní image generation MCP nebo reportovat capability_missing.]
Inputs: prompt string, entity type (logo/photo)
Outputs: vygenerovaný obrázek (base64 nebo URL), success/fail
Limitations: závisí na dostupnosti create_image nástroje; bez něj STOP

## logo-quality-checker
Task types: quality check pro týmová loga
Description: Ověří, že vygenerované logo splňuje pravidla GeneratorPrompt Ch 6:
  čtvercový formát, čisté minimalistické logo, žádný text/písmena/čísla,
  max 3 barvy, vizuálně konzistentní. Vrátí pass/fail s důvodem.
Inputs: vygenerovaný obrázek, očekávaný styl
Outputs: pass/fail, fail reason (pro prompt-refinement-handler)
Limitations: automatická detekce textu v obrázku je heuristická

## photo-quality-checker
Task types: quality check pro hráčské portréty
Description: Ověří, že vygenerovaná fotka splňuje pravidla GeneratorPrompt Ch 6:
  čtvercový formát, realistický styl, jeden hráč, chest-up kompozice,
  sportovní prostředí. Vrátí pass/fail s důvodem.
Inputs: vygenerovaný obrázek, typ entity
Outputs: pass/fail, fail reason (pro prompt-refinement-handler)
Limitations: detekce počtu osob je heuristická; při nejistotě → pass s nízkou confidence

-- D. RUN ORCHESTRATION (3) ------------------------------------

## generation-run-orchestrator
Task types: run orchestration, batch execution, time budget
Description: Orchestruje celý generation run: coverage → priority → load batch →
  per entity loop (verify → build prompt → generate → check → assign).
  Sleduje time budget a gracefully stops pokud hrozí timeout.
Inputs: entity type focus (Teams/Players/Auto), time budget
Outputs: execution plan, skipped steps, výsledky per entita
Limitations: nenahrazuje domain-specific logiku prompt buildingu

## image-assigner
Task types: přiřazení obrázku k entitě v USM, zápis URL nebo base64
Description: Uloží vygenerovaný obrázek k cílové entitě v USM.
  Před zápisem ověří: entita stále splňuje scope (IsBot, chybí obrázek).
  Použije upsert_record nebo update_record dle dostupnosti.
Inputs: EntityGuid, tabulka, vygenerovaný obrázek (URL nebo base64)
Outputs: zápis do DB, potvrzení
Limitations: VŽDY ověřit scope před zápisem; nikdy přepsat existující obrázek

## agent-schedule-manager
Task types: AgentSchedules, přehled agentů, přehled plánů (conflict detection zakázána)
Description: Spravuje tabulku AgentSchedules pro Generator agenty.
  Na konci runu: UPSERT (AgentName, ScheduledRunTime, NextRunAt, LastRunAt).
  Na startu: load přehled a zobrazit přehled plánů (conflict detection zakázána) ±1 min.
Inputs: AgentName, ScheduledRunTime, IntervalHours, AgentType="Generator"
Outputs: upsert do AgentSchedules, přehled agentů
Limitations: čas v Europe/Prague; NEZDVOJIT při DryRun

-- E. REPORTING & NOTIFICATIONS (3) ----------------------------

## generation-run-report-formatter
Task types: finální run report, formátování výstupu pro AgentRunReports
Description: Generuje strukturovaný run report: generatedCount, skippedCount,
  failedCount, entityTypeFocus, teamsCoverage, playersCoverage, errors, highlights.
Inputs: raw run data
Outputs: formátovaný report pro upsert_record("AgentRunReports",...)
Limitations: report musí být uložen i při selhání

## ntfy-notification-sender
Task types: ntfy notifikace, agent-runs, agent-errors, agent-alerts
Description: Odesílá markdown-formátované notifikace na ntfy.vo2info.cz.
  Generator-specific: coverage report, milestone alerts.
  Max 3 notifikace per run.
Inputs: kanál, zpráva, priority, tags
Outputs: notifikace odeslána
Limitations: max 3/run

## problem-report-writer
Task types: problem report, tab 'error' v master spreadsheet, nedokončený run
Description: Zapíše problem report do tabu 'error' v master spreadsheet {AgentName}
  pokud run selhal nebo self-audit našel chyby.
  Hledání: search_files(query="{AgentName}", folderId="{agentTypeFolderId}")
  Zápis: sheets_append_rows(spreadsheetId, range="error", rows=[...])
Inputs: AgentName, errors[] s ProblemScore, stav ntfy/Calendar/DB kroků
Outputs: append do tabu 'error' v master spreadsheet {AgentName}
Limitations: best-effort; Drive nedostupný → fallback AgentRunReports.Metadata

-- F. ADVANCED (3) -----------------------------------------------

## dry-run-mode-controller
Task types: DryRun mode, testovací běh bez zápisů a bez generování
Description: Řídí DryRun mód. Blokuje INSERT/UPDATE/DELETE a create_image.
  Simuluje výstupy s "[DRY_RUN]" prefixem. Povoluje DB čtení a Drive čtení.
Inputs: RunMode: dry_run nebo dry_run=true
Outputs: simulované výstupy, run report s IsDryRun=true
Limitations: Calendar eventy NEVYTVÁŘÍ; create_image NEVOLÁ

## next-run-calendar-scheduler
Task types: plánování příštího runu v Google Calendar, AI Generators
Description: Vytváří Calendar event pro příští scheduled run. CALENDAR_ID musí být vyřešen přes list_calendars() (hledat "AI Generators") — NIKDY nepoužívat "AI Generators" přímo jako calendar_id.
  list_events → smazat starý event → vytvořit nový.
Inputs: AgentName, výsledek runu, NextRunAt
Outputs: Calendar event vytvořen
Limitations: pokud list_calendars() selže → capability_missing("calendar_list_calendars"), přeskočit Calendar — NEBLOKUJ datový běh

## coverage-milestone-tracker
Task types: coverage milestone, 80%/90%/100% alerting
Description: Sleduje milníky pokrytí obrázků (80%, 90%, 100%) a odesílá ntfy alert
  při prvním dosažení milníku. Ukládá dosažené milníky do AgentRunReports.Metadata.
Inputs: teamsCoverage %, playersCoverage %, předchozí milestone stav
Outputs: ntfy alert pokud nový milestone dosažen, aktualizovaný milestone stav
Limitations: max 1 ntfy per milestone; nepřekračovat max 3 ntfy/run limit

=============================================================
PATCH 9.2.0 — SKILLS GOVERNANCE UPDATE
=============================================================
(additive patch — nenahrazuje existující skill definice; opravuje níže uvedená pravidla)

## agent-schedule-manager — 9.2.0 Update
(přepisuje ±1 min conflict window z původní definice)

Conflict detection: zakázána. Paralelní běh více agentů je povolený.
Inputs: AgentName, ScheduledRunTime, IntervalHours, AgentType
Outputs: upsert do AgentSchedules (POUZE při db_reachable), startup report konfliktů
Limitations: vkládat/aktualizovat AgentSchedules POUZE při db_reachable;
  reportovat schedule_drift_detected pokud DB plán ≠ Hermes plán

## problem-report-writer — 10.0.3 Update

Tab 'error' v master spreadsheet {AgentName} (VÝHRADNĚ, NIKDY samostatný soubor):
  Formát: ProblemScore | Category | Description | Timestamp | StepStatus
  Master spreadsheet: {AgentName} ve složce odpovídající AgentType (Generators složka)
Tab 'error' je součástí master spreadsheet — vytváří se při BOOTSTRAP spolu s ostatními taby.
  get_spreadsheet_range(spreadsheetId, range="error!A:Z") při startu
  sheets_append_rows(spreadsheetId, range="error", rows=[...]) při selhání
Pokud Drive write selže: neclaimovat uložení; uložit do AgentRunReports.Metadata pokud DB dostupná.
NEVYTVÁŘET samostatný soubor jako náhradu za tab 'error' v master spreadsheet.

## calendar-event-manager — 9.2.0 Update

NIKDY nepoužívat zobrazovaný název jako calendar_id parametr přímo.
VŽDY list_calendars() → hledat název odpovídající AgentType → uložit jako CALENDAR_ID proměnnou.
Pokud list_calendars() chybí: capability_missing("calendar_list_calendars") → přeskočit tiše.
Calendar failure = reporting degradation, ne datový blocker.

-- [G]. NOVÉ V 10.1.0 -----------------------------------------------

## mcp-version-validator
Task types: MCP version check, startup, minimum version, mcp_version_mismatch
Description: Ověřuje MCP_VERSION >= 10.0.1 při startupu. Pokud verze je nižší → hlásí mcp_version_mismatch v AgentRunReport, přeskočí všechny mutační operace (upsert, ensure_table, ensure_columns), pokračuje pouze v read-only módu.
Inputs: current MCP_VERSION (string), minimum required version (default "10.0.1")
Outputs: pass/fail, action_taken (proceed/skip_mutations), version_delta
Limitations: porovnává semver major.minor.patch; pre-release tagy ignoruje

## mcp-502-recovery-handler
Task types: 502 error, MCP nedostupnost, retry logika, db_502_permanent
Description: Zpracovává 502 Bad Gateway errory z MCP serveru. Exponential backoff: 1s → 2s → 4s, max 3 pokusy. Po 3 selháních nastaví db_502_permanent, přeskočí všechny DB operace pro daný run, pokračuje s Drive/API tasky. Loguje každý retry pokus.
Inputs: failed tool call, retry_count (aktuální), max_retries (default 3)
Outputs: retry result (success/fail), skip_decision (proceed/skip_db), total_wait_ms
Limitations: pouze pro 502 HTTP status; jiné chyby (404, 500, timeout) neřeší tímto flow

## agent-startup-health-broadcaster
Task types: ntfy, startup summary, readiness report, agent-status
Description: Po dokončení startup sekvence pošle na ntfy kanál agent-status stručné shrnutí: agent name, prompt verze, MCP verze, DB status (ok/unavailable), počet dostupných tabulek, prompt source (gitea/drive/cache), run ID. Pomáhá operátorovi sledovat stav všech agentů po jejich spuštění.
Inputs: všechny výsledky startup sekvence (version, db_status, tables_count, prompt_source, run_id)
Outputs: ntfy zpráva odeslaná na agent-status topic, startup_summary objekt pro AgentRunReport
Limitations: pokud ntfy nedostupné → přeskoč, loguj ntfy_unavailable, nepřerušuj startup

## governance-prompt-cache-sync
Task types: AgentPromptCache, verze synchronizace, cache invalidace, cache_refreshed
Description: Po načtení nové verze governance promptu z Drive nebo Gitea aktualizuje záznam v AgentPromptCache tabulce. Ověřuje, že cached PromptVersion odpovídá právě načtené. Pokud ne → přepíše cache novým obsahem a loguje cache_refreshed v Metadata.
Inputs: agent_name, prompt_content, new_version, source (gitea/drive/github)
Outputs: cache_status (hit/miss/refreshed), previous_version, new_version
Limitations: cache write může selhat pokud DB nedostupná — v takovém případě přeskoč cache update a pokračuj

## skill-changelog-recorder
Task types: audit skills, changelog, verzování, skill_added/skill_modified/skill_removed
Description: Loguje přidání, úpravu nebo odebrání skills. Záznam obsahuje: datum, skill_name, change_type (added/modified/removed), reason, operator. Záznamy ukládá do sekce SKILL_CHANGELOG na konci GeneratorPromptSkills.txt a do AgentRunReport jako metadata.
Inputs: skill_name, change_type, reason, operator (volitelně)
Outputs: changelog entry s datumem, status (logged/error)
Limitations: pouze loguje — nespouští žádné akce automaticky

## drive-file-version-selector
Task types: Drive soubory, více verzí, newest selection, governance, drive_file_multiple_versions
Description: Při načítání governance souborů z Drive složky vyhledá všechny soubory odpovídající vzoru (např. "GeneratorPrompt") a vybere ten s nejvyšším VersionNumber: z obsahu (ne z názvu souboru). Zabrání použití zastaralé verze. Pokud existuje více verzí → reportuje drive_file_multiple_versions.
Inputs: folder_id, filename_pattern, version_field (default "VersionNumber:")
Outputs: vybraný file_id, verze, datum, seznam ignorovaných souborů s jejich verzemi
Limitations: musí číst obsah každého souboru pro verzi — pomalé při >10 souborech

## Skills Version Policy (9.2.0)

SkillsVersion: 10.1.0
Minimální počet aktivních skills pro plný run: záleží na AgentType (viz PromptFile).
Pokud skills soubor nebyl načten z Drive: capability_missing("skills_file_unavailable") → degraded mode.
