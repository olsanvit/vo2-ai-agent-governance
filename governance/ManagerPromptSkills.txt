SkillsFor: ManagerPrompt
SkillsVersion: 10.0.1
Generated: 2026-06-17

Skills jsou konfigurovány per agent v Claude.ai — konkrétní sada dostupná agentovi se může lišit.
Tento soubor je autoritativní seznam skills povolených pro Manager agenty.
Každý Manager agent musí mít aktivních ASPOŇ 11 skills.

!! MAINTENANCE POZNÁMKA: Přibližně 60 skills existuje identicky v ManagerPromptSkills i
   CatalogPromptSkills. Při úpravě sdíleného skilu VŽDY aktualizuj OBA soubory.
   Viz CatalogPromptSkills.txt pro úplný seznam sdílených skills.

=============================================================
PRAVIDLA POUŽITÍ SKILLS
=============================================================

- pokud skill pokrývá úkol: použít skill, má přednost před obecným workflow
- pokud skill odporuje canonical governance: canonical governance má přednost
- pokud skill není dostupný v Claude.ai: pokračovat dle ManagerPrompt governance, reportovat capability_missing
- nikdy netvrdit, že skill existuje pokud není dostupný
- uvést SkillsUsed[] v run reportu
- skill NENÍ v tomto Skill Directory: ZABLOKOVAT použití, reportovat skill_blocked
- nezkoušet použít skill který není v tomto seznamu, i kdyby byl dostupný v Claude.ai
- pokud úkol není pokryt žádným skill: navrhnout nový skill ve formátu Skill Directory
  (zejména sport-specific), přidat ho do ManagerPromptSkills.txt na Drive
  a reportovat jako capability_missing

=============================================================
SKILL DIRECTORY (126 skills)
=============================================================

-- A. DB & SCHEMA (8) ------------------------------------------

## db-schema-guardian
Task types: schema governance, tabulky, sloupce, indexy, BaseGuid, migrace
Description: Autoritativní správce DB schématu. Zajišťuje, že všechny tabulky mají povinné
  sloupce (Guid, CreatedAt, UpdatedAt, Emoji, Colors, scoring sloupce), správné indexy,
  FK ve formátu SomethingGuid a BaseGuid pro multi-type entity. Řídí migrace bezpečně.
Inputs: název tabulky nebo seznam tabulek, požadované schéma změny
Outputs: SQL migration script, schema diff report, bezpečnostní hodnocení migrace
Limitations: nespouští migrace automaticky — vždy vyžaduje potvrzení operátora

## db-migration-validator
Task types: validace SQL migrací, safety check před spuštěním
Description: Analyzuje SQL migration skripty před spuštěním. Detekuje destruktivní operace
  (DROP, TRUNCATE, ALTER bez NULL default), chybějící sloupce, FK porušení.
Inputs: SQL migration script, aktuální schema
Outputs: seznam rizik (CRITICAL/WARNING/INFO), doporučení oprav
Limitations: nenahrazuje testování na dev DB — vždy otestovat před produkcí

## db-null-audit
Task types: NULL analýza, datová kvalita, prioritizace backfillu
Description: Skenuje sportovní tabulky a reportuje NULL hodnoty per sloupec seřazené
  dle NULL ratio DESC. Prioritizuje: Matches, Teams, Players, Competitions first.
Inputs: seznam tabulek nebo ALL, min NULL ratio threshold (default 5%)
Outputs: NULL audit report (tabulka, sloupec, NULL count, NULL ratio, priority rank)
Limitations: max 200 tabulek na run; neanalyzuje JSONB vnořené hodnoty

## db-index-optimizer
Task types: indexy, výkon, missing/redundant indexes
Description: Identifikuje chybějící indexy na FK a filter sloupcích sportovních tabulek
  (MatchDate, CompetitionGuid, SeasonGuid...). Navrhuje CREATE/DROP INDEX příkazy.
Inputs: seznam tabulek nebo ALL
Outputs: doporučení CREATE INDEX, DROP INDEX, estimated performance gain
Limitations: navrhuje pouze, nespouští — DBA potvrzení povinné

## db-constraint-checker
Task types: FK constraints, unique constraints, sport data integrity
Description: Validuje FK constraints v sportovních tabulkách: HomeTeamGuid/AwayTeamGuid
  musí existovat v Teams, CompetitionGuid v Competitions, SeasonGuid v Seasons.
Inputs: seznam tabulek nebo ALL
Outputs: seznam violations s tabulka/sloupec/hodnota, doporučení oprav
Limitations: pouze čte; neopravuje data automaticky

## db-table-health-monitor
Task types: monitoring tabulek, row counts, freshness, bloat
Description: Monitoruje zdraví sportovních tabulek: počty řádků, datum posledního UpdatedAt,
  table bloat. Flaguje stale tabulky dle SLA (Matches ImportantScore 100 = max 24h).
Inputs: seznam tabulek, SLA konfigurace
Outputs: health report per tabulka (rows, last_update, bloat_estimate, sla_status)
Limitations: bloat estimate přibližný bez VACUUM ANALYZE

## db-column-score-auditor
Task types: scoring sloupce, ConfidenceScore, ReliabilityScore, sport data
Description: Ověřuje, že scoring sloupce jsou populovány ve všech sportovních záznamech.
  Prioritizuje Matches a Players tabulky pro NULL violations.
Inputs: seznam tabulek nebo ALL
Outputs: NULL count per scoring sloupec, seznam Guidů s chybějícím score
Limitations: max 100 oprav na run; neopravuje skóre bez zdroje dat

## db-foreign-key-resolver
Task types: dangling FK, osiřelé záznamy, sport referenční integrita
Description: Hledá dangling FK v sportovních tabulkách: zápasy s neexistujícím týmem,
  hráči bez Squad záznamu, Standings bez platné Competition/Season kombinace.
Inputs: tabulka, seznam FK sloupců
Outputs: seznam dangling FK s Guid a návrhem opravy
Limitations: neopravuje automaticky; vždy vyžaduje review

-- B. MATCH CORE (12) ------------------------------------------

## match-upsert-guardian
Task types: identity validace, matching, safe upsert zápasů a entit
Description: Bezpečný upsert s validací identity. Provádí multi-step matching
  (exact name → alias → token 80% → ExternalID → manual queue) před zápisem.
  Pro sport: validuje HomeTeam/AwayTeam/Competition/Season konzistenci před upsert Matches.
  POZNÁMKA: Tento skill je dostupný jako instrukce z načteného ManagerPromptSkills.txt.
  Není to separátní MCP tool — nelze "attach" nebo "detach". Vždy dostupný pokud skills soubor načten.
Inputs: entita nebo zápas k upsertnutí s dostupnými identifikátory
Outputs: match result (matched/new/conflict), Guid, upsert výsledek
Limitations: při nejednoznačném matchování posílá do ManualReviewQueue, nezapisuje

## flashscore-match-harvester
Task types: sběr zápasů z Flashscore-like zdrojů, live score pages
Description: Specializovaný harvester pro Flashscore a podobné sportovní výsledkové weby.
  Parsuje výsledky, datum, týmy, skóre, status zápasu. Respektuje rate limiting (1 req/s),
  circuit breaker a per-date limity (max 100 nových zápasů, max 25 dotazů na datum).
Inputs: cílové datum nebo rozsah datumů, sport/competition filter
Outputs: seznam zápasů s HomeTeam, AwayTeam, Score, Status, StartTimeUtc, SourceUrl
Limitations: pouze historické zápasy (včera a starší); dnešní a budoucí PŘESKOČIT

## match-result-validator
Task types: validace výsledků, score consistency, anomaly detection
Description: Validuje výsledky zápasů: HomeScore/AwayScore rozsah (0-30 standard),
  flag pro >15 gólů (manual review), konzistence se MatchEvents (gól count).
Inputs: MatchGuid, HomeScore, AwayScore, MatchEvents count
Outputs: validace výsledek, anomaly flags (high_score/events_mismatch)
Limitations: flag pouze informativní; nezakazuje zápis; manual review queue pro anomálie

## match-status-tracker
Task types: Match status, lifecycle (scheduled→live→finished→cancelled)
Description: Spravuje lifecycle status zápasů: scheduled → live → finished → cancelled/postponed.
  Validuje přechody (nelze přejít z finished na scheduled). Loguje změny.
Inputs: MatchGuid, nový status, timestamp
Outputs: aktualizovaný status, validace přechodu (allowed/blocked), AuditLog
Limitations: pouze validní přechody; nelze vrátit finished zápas na live

## match-duplicate-detector
Task types: detekce duplicitních zápasů, HomeTeam/AwayTeam/Date unikátnost
Description: Detekuje potenciální duplicitní zápasy: stejný HomeTeam + AwayTeam +
  MatchDate + Competition. Vrací kandidáty na merge seřazené dle shody.
Inputs: zápas k insertu (HomeTeamGuid, AwayTeamGuid, MatchDate, CompetitionGuid)
Outputs: seznam potenciálních duplikátů s match score, doporučení (insert/merge/skip)
Limitations: neřeší rescheduled zápasy (jiné datum); pouze detekuje

## match-date-resolver
Task types: datum zápasu, timezone, UTC konverze, Prague TZ
Description: Resolvuje datum zápasu z různých zdrojů (local time, UTC, timezone-aware).
  Konvertuje na StartTimeUtc. Ověřuje, že datum je ≤ včerejšek (Prague TZ).
Inputs: raw datum/čas ze zdroje, source timezone
Outputs: StartTimeUtc (ISO 8601 UTC), MatchDate (date part), is_historical flag
Limitations: zápasy dnes nebo v budoucnu → PŘESKOČIT; nepracuje s live zápasy

## match-score-anomaly-checker
Task types: score anomálie, high score flag, manual review
Description: Flaguje zápasy se skóre mimo normální rozsah: HomeScore nebo AwayScore > 15.
  Vytváří ManualReviewQueue záznam pro lidský dohled.
Inputs: MatchGuid, HomeScore, AwayScore
Outputs: anomaly flag (true/false), ManualReviewQueue záznam (pokud flagován)
Limitations: threshold 15 je konfigurovatelný; flag nezastavuje zápis

## match-event-bulk-inserter
Task types: MatchEvents, smart_upsert_batch, goals/cards/substitutions
Description: Efektivně bulk insertuje MatchEvents (góly, karty, střídání) pro zápas.
  Substitution = 2 záznamy (IN + OUT). Deduplicates dle EventType/Minute/PlayerGuid.
Inputs: MatchGuid, seznam eventů (type, minute, player, team)
Outputs: počet vložených eventů, duplicity přeskočeny, smart_upsert_batch výsledek
Limitations: max 100 eventů per zápas; dedup pouze na základě minute + type + player

## match-lineups-processor
Task types: MatchLineups, startovní XI, lavička, MinutesPlayed
Description: Zpracovává startovní sestavy a lavičky. Validuje počty hráčů (max 11 XI,
  max 12 lavička pro fotbal). Ukládá pozici, číslo, MinutesPlayed.
Inputs: MatchGuid, seznam hráčů (PlayerGuid, position, jersey_number, is_starter)
Outputs: MatchLineups záznamy, validace (správné počty), nerozpoznaní hráči
Limitations: neresolvuje PlayerGuidy automaticky — musí být předem matchnuté

## match-stats-aggregator
Task types: MatchStats, possession, shots, corners, xG, aggregation
Description: Agreguje statistiky zápasu do MatchStats tabulky: possession, strely,
  rohy, fauly, xG, úspěšnost přihrávek. 2 záznamy per zápas (Home + Away).
Inputs: MatchGuid, home stats, away stats (raw values from source)
Outputs: 2 MatchStats záznamy (HomeTeam + AwayTeam), upsert výsledek
Limitations: neodhaduje chybějící stats; NULL pro nedostupné hodnoty

## match-odds-recorder
Task types: MatchOdds, opening/closing odds, bookmakers
Description: Ukládá pre-match odds z bookmakerů (1/X/2, Over/Under).
  Zaznamenává opening a closing odds. Vypočítává implied probability.
Inputs: MatchGuid, bookmaker, odds_type (1X2/OU), opening/closing values
Outputs: MatchOdds záznamy, implied probability (1/total_odds)
Limitations: pouze pre-match odds; live odds neukládá; max 5 bookmakerů per zápas

## match-coverage-auditor
Task types: coverage audit, post-match completeness, pipeline status
Description: Audituje completeness post-match dat pro zápasy: zda má Tier1/2/3 data.
  Generuje seznam zápasů s chybějícími daty seřazený dle priority.
Inputs: seznam zápasů (MatchGuids), CompetitionGuid, SeasonGuid
Outputs: coverage report (Tier1/2/3 status per match), priority list pro doplnění
Limitations: pouze reportuje; nespouští doplnění automaticky

-- C. COMPETITION & SEASON (10) --------------------------------

## season-lifecycle-manager
Task types: season lifecycle, nova/ukončená sezona, IsActive flag
Description: Detekuje začátek a konec sezóny. Nastavuje IsActive flag. Při ukončení
  archivuje Season záznam. Validuje, zda jsou všechny Standings finalizovány.
Inputs: CompetitionGuid, aktuální datum
Outputs: Season status (active/finished/upcoming), archivační akce
Limitations: neuznává ukončení sezóny pokud chybí finálové zápasy

## competition-hierarchy-mapper
Task types: hierarchie soutěží, parent/child competitions, zones
Description: Mapuje hierarchii soutěží: national league → division → group.
  Udržuje ParentCompetitionGuid vazby a CompetitionZones konfigurace.
Inputs: CompetitionGuid, hierarchy definition
Outputs: aktualizovaná hierarchie, CompetitionZones záznamy
Limitations: max 5 úrovní hierarchie; cyklické závislosti blokuje

## season-date-validator
Task types: datum sezóny, StartDate/EndDate konzistence
Description: Validuje data sezóny: StartDate < EndDate, všechny zápasy v sezóně
  musí mít MatchDate v rozmezí sezóny. Flaguje outlier zápasy.
Inputs: SeasonGuid, StartDate, EndDate
Outputs: validace výsledek, outlier zápasy mimo sezónu
Limitations: pouze validuje; neopravuje MatchDate zápasů automaticky

## group-stage-coordinator
Task types: skupinová fáze, group assignments, intra-group standings
Description: Spravuje skupinovou fázi turnajů: přiřazení týmů do skupin,
  výpočet standings per skupina, přestup do knockout fáze.
Inputs: CompetitionGuid, SeasonGuid, group definitions (A/B/C...)
Outputs: group assignments, intra-group Standings, postup/vypadnutí klasifikace
Limitations: pouze pro soutěže s explicitní group stage konfigurací

## knockout-phase-tracker
Task types: knockout, play-off, pavouk, bracket management
Description: Trackuje knockout fázi soutěže: páry, výsledky, postupující.
  Spravuje bracket od čtvrtfinále po finále včetně rozehry o 3. místo.
Inputs: CompetitionGuid, SeasonGuid, knockout round definice
Outputs: bracket update, postupující týmy, finálové výsledky
Limitations: neřeší re-match pravidla automaticky; vyžaduje explicitní konfiguraci

## promotion-relegation-handler
Task types: postup, sestup, promoted/relegated flag, Standings finalizace
Description: Po skončení sezóny nastaví promoted/relegated/playoff flags na Standings
  záznamech dle tiebreaker pravidel a kompetitiv konfigurace.
Inputs: CompetitionGuid, SeasonGuid, promotion/relegation spots count
Outputs: aktualizované Standings s promoted/relegated flags, playoff spot flags
Limitations: vyžaduje kompletní Standings; nelze aplikovat na nedokončenou sezónu

## competition-zone-manager
Task types: CompetitionZones, UEFA/FIFA zones, continental groups
Description: Spravuje geografické a kompetitivní zóny soutěží (UEFA Champions League
  groups, FIFA confederations). Udržuje ZoneType a ZoneName.
Inputs: CompetitionGuid, zone definitions
Outputs: CompetitionZones záznamy, team-to-zone assignments
Limitations: pouze pro soutěže s explicitní zone konfigurací

## tiebreaker-rule-loader
Task types: tiebreaker pravidla, Competition.TiebreakerJson, standings pořadí
Description: Načítá a parsuje tiebreaker pravidla z Competition.TiebreakerJson.
  Podporuje: body → GD → GF → H2H → žluté karty → los.
Inputs: CompetitionGuid
Outputs: parsed tiebreaker rule chain, validace completeness
Limitations: pouze pravidla explicitně definovaná v TiebreakerJson; bez defaults

## season-squad-validator
Task types: squad validace, Transfer Window, squad changes governance
Description: Validuje změny v Squads dle stavu Transfer Window (IsCurrentlyOpen).
  Zakazuje squad changes pokud Transfer Window uzavřeno bez explicitního potvrzení.
Inputs: SeasonGuid, zamýšlená squad change, Transfer Window stav
Outputs: allow/block rozhodnutí, důvod při blokování
Limitations: emergency exceptions vyžadují manuální override s dokumentací

## competition-coverage-tracker
Task types: coverage tracking, competition completeness, missing matches
Description: Porovnává počet očekávaných zápasů (dle formátu soutěže) s počtem v DB.
  Identifikuje chybějící zápasy per round/matchday.
Inputs: CompetitionGuid, SeasonGuid, expected format (teams count, rounds)
Outputs: coverage % per round, seznam chybějících matchday dat
Limitations: vyžaduje znalost formátu soutěže (round-robin, knockout)

-- D. TEAMS & PLAYERS (12) ------------------------------------

## squad-update-guardian
Task types: Squads, hráčské soupisy, Transfer Window governance
Description: Bezpečně aktualizuje Squad záznamy (IsActive, JoinDate, LeaveDate).
  Respektuje Transfer Window stav. Loguje každou změnu do AuditLog.
Inputs: PlayerGuid, TeamGuid, SeasonGuid, change type (join/leave/loan)
Outputs: Squad záznam (upsert), Transfer Window validation result
Limitations: Transfer Window closed = blokuje join/leave bez potvrzení

## player-transfer-processor
Task types: transfery, Transfers tabulka, squad aktualizace
Description: Zpracovává přestupy hráčů: vytváří Transfers záznam, aktualizuje Squads
  (starý tým LeaveDate, nový tým JoinDate). Validuje Transfer Window.
Inputs: PlayerGuid, from TeamGuid, to TeamGuid, transfer_date, fee (volitelně)
Outputs: Transfers záznam, aktualizované Squads záznamy (oba týmy)
Limitations: pouze historické transfery (datum ≤ dnes); budoucí transfery neukládá

## player-availability-tracker
Task types: PlayerUnavailability, zranění, suspenze, dostupnost
Description: Spravuje PlayerUnavailability záznamy: vytváří a uzavírá záznamy o
  nedostupnosti (zranění/suspenze/nemoc). Trackuje expected return date.
Inputs: PlayerGuid, reason (injury/suspension/illness), start_date, expected_end
Outputs: PlayerUnavailability záznam, aktualizace IsAvailable flag na Squad
Limitations: bez lékařské diagnózy pouze reason kategorie; neodhaduje délku absence

## player-suspension-checker
Task types: suspenze, disciplinární zákazy, PlayerDiscipline
Description: Počítá akumulované disciplinární body (žluté karty) a detekuje automatické
  suspenze dle competition pravidel. Nastavuje suspend flag při dosažení thresholdu.
Inputs: PlayerGuid, CompetitionGuid, SeasonGuid, YellowCard threshold
Outputs: aktuální YC count, suspend flag (true/false), matches to serve
Limitations: threshold čte z Competition konfigurace; bez konfigurace používá default 5

## team-formation-recorder
Task types: Formation, taktika, 4-3-3/4-4-2/3-5-2
Description: Zaznamenává taktické formace týmů per zápas do MatchLineups.Formation.
  Parsuje formace ve standardním formátu (4-3-3, 4-4-2, 3-5-2...).
Inputs: MatchGuid, TeamGuid, formation string
Outputs: aktualizovaný MatchLineups záznam s Formation
Limitations: pouze validní formace (součet = 10 outfield); brankář se nepočítá

## player-position-normalizer
Task types: pozice hráčů, normalizace, canonical position codes
Description: Normalizuje různé zdroje pozic hráčů na canonical kódy: GK, DEF, MID, FWD,
  případně detailnější CB, LB, RB, CDM, CAM, LW, RW, ST.
Inputs: raw position string ze zdroje, normalization config
Outputs: canonical position code, normalized position string
Limitations: neznámé pozice mapuje na nejbližší canonical; log jako warning

## player-nationality-resolver
Task types: národnost hráče, ISO 3166, dual nationality
Description: Resolvuje národnost hráče na ISO 3166-1 alpha-2 kódy. Zpracovává
  dual nationality (max 2 národnosti). Aktualizuje Players.Nationality.
Inputs: PlayerGuid, nationality string(s) ze zdroje
Outputs: ISO 3166 kódy, aktualizovaný Players záznam
Limitations: max 2 národnosti per hráč; nerozlišuje eligibility pro různé federace

## team-color-scheme-updater
Task types: Colors tabulka, dresy, home/away barvy
Description: Aktualizuje barevné schéma týmu v Colors sloupci/tabulce.
  Home a away barvy ve formátu HEX (#RRGGBB). Deduplikuje shodná schémata.
Inputs: TeamGuid, home_color (HEX), away_color (HEX), season (volitelně)
Outputs: aktualizovaný Colors záznam, dedup check
Limitations: pouze HEX formát; neanalyzuje obrázky dresů automaticky

## player-age-calculator
Task types: věk hráče, BirthDate, age_at_match
Description: Vypočítává věk hráče k datu zápasu nebo k aktuálnímu datu.
  Ukládá age_at_match do MatchLineups.Metadata.
Inputs: PlayerGuid, BirthDate, reference_date (zápas nebo dnes)
Outputs: vypočítaný věk (roky), age_at_match pro MatchLineups
Limitations: vyžaduje BirthDate; bez BirthDate vrátí null

## player-career-stats-aggregator
Task types: PlayerSeasonStats, career agregace, statistiky per sezóna
Description: Agreguje celosezónní statistiky hráče do PlayerSeasonStats: góly, asistence,
  minuty, zápasy, yellow/red cards. Vypočítává per-90 metriky.
Inputs: PlayerGuid, SeasonGuid, CompetitionGuid (volitelně)
Outputs: PlayerSeasonStats záznam (upsert), per-90 metriky
Limitations: agregace pouze z MatchEvents a MatchLineups; neimportuje ze zdrojů

## team-venue-association
Task types: tým a venue, HomeVenueGuid, Venues tabulka
Description: Asociuje tým s jeho domácím stadionem. Aktualizuje Teams.HomeVenueGuid
  a vytváří Venues záznam pokud neexistuje. Trackuje historické změny stadionu.
Inputs: TeamGuid, venue name, city, capacity (volitelně)
Outputs: Venues Guid, aktualizovaný Teams.HomeVenueGuid
Limitations: historické změny stadionu ukládá do Metadata; jeden aktivní venue

## coach-assignment-tracker
Task types: trenéři, Coaches, přiřazení k týmům, CareerHistory
Description: Trackuje přiřazení trenérů k týmům: vytváří Coaches záznam, aktualizuje
  Teams.HeadCoachGuid, ukládá datum nástupu/odchodu do Metadata.
Inputs: CoachGuid nebo jméno, TeamGuid, assignment_date, end_date (volitelně)
Outputs: Coaches záznam, aktualizovaný Teams.HeadCoachGuid, career history entry
Limitations: pouze jeden head coach per tým v daný čas; asistenti do Metadata

-- E. STANDINGS & STATS (10) -----------------------------------

## standings-calculator
Task types: výpočet tabulek, tiebreaker, Standings upsert
Description: Přepočítává ligové tabulky po každém zápase. Čte tiebreaker pravidla
  z Competition.TiebreakerJson (body → GD → GF → H2H → ...) a aplikuje v správném pořadí.
  Ukládá Standings s pozicí, body, W/D/L, GF/GA/GD pro každý tým v sezoně.
Inputs: CompetitionGuid, SeasonGuid, seznam zápasů ke zpracování
Outputs: upsert Standings záznamů, tiebreaker log, změny pozic (promoted/relegated flag)
Limitations: neřeší play-off ani skupinové fáze bez explicitní konfigurace v TiebreakerJson

## player-form-tracker
Task types: výpočet formy, TeamForm, PlayerForm, rolling stats
Description: Počítá rolling formu hráčů a týmů (posledních 5 a 10 zápasů).
  TeamForm: W/D/L, góly, body na zápas, trend (improving/declining/stable).
  PlayerForm: góly, asistence, rating, minuty — agregace přes MatchLineups + MatchEvents.
Inputs: EntityGuid (tým nebo hráč), počet posledních zápasů (5 nebo 10), datum výpočtu
Outputs: upsert TeamForm / PlayerForm záznamu s FormString (např. "WDWLW"), TrendScore
Limitations: min. 3 zápasy pro výpočet formy; neřeší mezinárodní přestávky jako výpadek

## top-scorer-tracker
Task types: střelci, top scorers, góly per sezóna/soutěž
Description: Sestavuje a aktualizuje tabulku střelců per Competition + Season.
  Počítá góly z MatchEvents (EventType = goal, excludes own goals).
Inputs: CompetitionGuid, SeasonGuid
Outputs: seřazený seznam hráčů (PlayerGuid, goals, xG volitelně)
Limitations: pouze zápasy ve stavu Finished; neodhaduje nedetekované góly

## clean-sheet-tracker
Task types: clean sheets, brankáři, nulové zápasy, GK statistiky
Description: Trackuje clean sheets pro brankáře a týmy per Competition + Season.
  Clean sheet = 0 obdržených gólů za celý zápas (bez OG).
Inputs: CompetitionGuid, SeasonGuid
Outputs: clean sheet tabulka (TeamGuid/PlayerGuid, count, percentage)
Limitations: vyžaduje MatchLineups pro identifikaci brankáře; bez lineup = team-only stat

## possession-stats-recorder
Task types: possession, MatchStats, possession% per tým
Description: Zaznamenává a validuje possession statistiky (must sum to 100%).
  Ukládá do MatchStats (2 záznamy per zápas). Flaguje neplatné hodnoty.
Inputs: MatchGuid, home_possession (%), away_possession (%)
Outputs: MatchStats záznamy, validace (home + away = 100%)
Limitations: tolerance ±1% pro součet; bez dat ukládá NULL, ne odhad

## xg-data-processor
Task types: xG, expected goals, MatchStats, player xG
Description: Zpracovává xG (expected goals) data ze zdrojů.
  Ukládá team xG do MatchStats, player xG do MatchEvents nebo PlayerSeasonStats.
Inputs: MatchGuid, home_xG, away_xG, player xG list (volitelně)
Outputs: MatchStats s xG, MatchEvents s player xG (pokud k dispozici)
Limitations: xG není standardně dostupné ze všech zdrojů; NULL pokud nedostupné

## disciplinary-stats-aggregator
Task types: disciplíny, žluté/červené karty, PlayerDiscipline
Description: Agreguje disciplinární statistiky: YC/RC per hráč per sezóna.
  Aktualizuje PlayerDiscipline tabulku. Volá player-suspension-checker při thresholdu.
Inputs: CompetitionGuid, SeasonGuid
Outputs: PlayerDiscipline záznamy, suspend flag triggers, YC/RC leaderboard
Limitations: pouze z MatchEvents (EventType = yellow_card / red_card)

## home-away-record-calculator
Task types: domácí/venkovní statistiky, Team H/A split, performance split
Description: Počítá domácí a venkovní záznam pro každý tým per sezóna.
  Výsledky: W/D/L home, W/D/L away, body, GF/GA separátně.
Inputs: TeamGuid, SeasonGuid (volitelně CompetitionGuid)
Outputs: home record + away record jako JSON, upsert do TeamForm.Metadata
Limitations: min 3 domácí/venkovní zápasy pro statistiku; méně = insufficient flag

## head-to-head-updater
Task types: H2H, HeadToHead tabulka, vzájemné zápasy
Description: Aktualizuje HeadToHead záznamy po každém zápase mezi dvěma týmy.
  Ukládá: W/D/L, celkové góly, posledních 5 vzájemných zápasů, home/away split.
Inputs: MatchGuid, HomeTeamGuid, AwayTeamGuid, výsledek
Outputs: HeadToHead záznam (upsert), posledních 5 setkání update
Limitations: symetrický záznam (A vs B = B vs A); max 50 historických zápasů v H2H

## assists-leaderboard-updater
Task types: asistence, assists leaderboard, PlayerSeasonStats
Description: Aktualizuje tabulku asistentů per Competition + Season.
  Počítá asistence z MatchEvents (EventType = assist).
Inputs: CompetitionGuid, SeasonGuid
Outputs: seřazený seznam hráčů (PlayerGuid, assists), PlayerSeasonStats update
Limitations: pouze explicitní assist eventy; neodhaduje asistence bez source dat

-- F. ODDS & INTEGRITY (8) -------------------------------------

## odds-integrity-monitor
Task types: sledování odds, integrity flag, pohyb > 20 %, ManualReviewQueue
Description: Analyzuje pohyb sázkových kurzů mezi opening a closing odds.
  Při pohybu > 20 % na jakémkoliv výsledku (1/X/2) vytvoří integrity flag a záznam
  v ManualReviewQueue pro manuální kontrolu. Loguje implied probability pro každý zápas.
Inputs: MatchGuid, opening odds, closing odds (1/X/2 nebo Over/Under)
Outputs: integrity flag (true/false), ManualReviewQueue záznam, implied probability záznam
Limitations: pouze pre-match odds; live odds nesleduje; threshold 20 % konfigurovatelný

## opening-odds-recorder
Task types: opening odds, první kurzy, market open
Description: Zaznamenává opening odds (kurzy při otevření marketu) z bookmakerů.
  Ukládá do MatchOdds s odds_type=opening a bookmaker identifikátorem.
Inputs: MatchGuid, bookmaker, 1X2 odds nebo OU odds
Outputs: MatchOdds záznamy (opening), timestamp otevření marketu
Limitations: opening odds pouze jednou per bookmaker per zápas; nepřepisuje

## closing-odds-analyzer
Task types: closing odds, line movement, market close
Description: Zaznamenává closing odds (kurzy těsně před začátkem zápasu) a porovnává
  s opening odds. Počítá % pohyb pro každý outcome.
Inputs: MatchGuid, bookmaker, closing 1X2 nebo OU odds
Outputs: MatchOdds záznamy (closing), line movement % per outcome
Limitations: closing odds pouze pro zápasy se zaznamenaným opening; bez opening = skip

## implied-probability-calculator
Task types: implied probability, overround, fair odds
Description: Vypočítává implied probability z kurzů (1/odds normalizovaná na 100%).
  Počítá overround (bookmaker marži). Ukládá do MatchOdds.Metadata.
Inputs: 1X2 nebo OU odds (decimal)
Outputs: implied probability per outcome, overround %, fair odds (bez marže)
Limitations: pouze decimal odds; fractional/american vyžadují konverzi

## line-movement-tracker
Task types: line movement, odds trend, sharp money detection
Description: Trackuje historii pohybu kurzů mezi opening a closing. Identifikuje
  neočekávané pohyby (> 20%) jako potenciální sharp money signal.
Inputs: MatchGuid, bookmaker, odds history (chronologická sekvence)
Outputs: line movement graf data, sharp money flags, significant moves list
Limitations: vyžaduje min 2 odds snapshoty; pouze z bookmakerů s history dostupností

## bookmaker-reliability-scorer
Task types: spolehlivost bookmakerů, data quality, source scoring
Description: Hodnotí spolehlivost bookmakerských zdrojů dle: dostupnosti odds,
  přesnosti opening odds (vs closing), rychlosti publikace.
Inputs: bookmaker name, historical odds comparison data
Outputs: bookmaker reliability score (0-100), trend, doporučení pro prioritizaci
Limitations: min 20 zápasů pro výpočet reliability score

## integrity-flag-escalator
Task types: integrity escalation, ManualReviewQueue, ntfy alert
Description: Eskaluje integrity flags (odds anomálie, score anomálie) na ManualReviewQueue
  a odesílá ntfy notifikaci na agent-maintenance kanál.
Inputs: flag type, MatchGuid, anomaly detail
Outputs: ManualReviewQueue záznam, ntfy notifikace, escalation log
Limitations: max 3 ntfy per run; sdílí limit s ostatními notification skills

## arbitrage-detector
Task types: arbitrage, cross-bookmaker, guaranteed profit detection
Description: Detekuje arbitrážní příležitosti porovnáním kurzů napříč bookmakers.
  Flaguje situace kde součet reciprokých kurzů < 1.00 (guaranteed profit).
Inputs: MatchGuid, odds per bookmaker (1X2 nebo OU)
Outputs: arbitrage flag (true/false), výpočet profit margin, stake distribution
Limitations: pouze informativní flag; nenavrhuje konkrétní sázky

-- G. POST-MATCH PIPELINE (12) ---------------------------------

## tier1-pipeline-executor
Task types: Tier1 kroky, okamžité post-match, scores/events/standings
Description: Exekuuje Tier1 post-match pipeline kroky okamžitě po dokončení zápasu:
  HomeScore/AwayScore/Status/WinnerTeamGuid, MatchEvents smart_upsert_batch, Standings update.
Inputs: MatchGuid, raw result data ze zdroje
Outputs: Matches update, MatchEvents záznamy, Standings update, AuditLog
Limitations: Tier1 NIKDY přeskočit; při selhání retry → fallback → error escalation

## tier2-pipeline-scheduler
Task types: Tier2 kroky, do 24h, attendance/stats/lineups/odds
Description: Plánuje a exekuuje Tier2 kroky pro zápasy (do 24h po zápase):
  Attendance, RefereeGuid, MatchStats, MatchLineups, MatchOdds (closing).
Inputs: MatchGuid, dostupnost dat (attendance/stats/lineups/odds)
Outputs: Tier2 záznamy vytvořeny/aktualizovány, chybějící data flagována
Limitations: Tier2 kroky lze přeskočit v fallback módu; odložit max na 48h

## tier3-pipeline-planner
Task types: Tier3 kroky, do 7 dní, ratings/H2H/form
Description: Plánuje Tier3 kroky pro zápasy (do 7 dní): player ratings, individual stats,
  H2H update, TeamForm + PlayerForm (rolling 5 a 10 zápasů).
Inputs: MatchGuid, dostupnost Tier3 dat
Outputs: Tier3 execution plan, scheduled timestamps, completion tracking
Limitations: Tier3 lze odložit nebo přeskočit v degraded módu; min priority

## attendance-recorder
Task types: návštěvnost, Attendance, MatchStats
Description: Zaznamenává návštěvnost zápasu do MatchStats nebo Matches.Attendance.
  Validuje rozsah (0 - max kapacita stadionu). Flaguje anomálie.
Inputs: MatchGuid, attendance value, stadium capacity (volitelně)
Outputs: aktualizovaný Attendance záznam, over-capacity flag pokud > kapacita
Limitations: toleruje 10% over-capacity (VIP/media); bez kapacity nelze validovat

## referee-performance-tracker
Task types: rozhodčí, Referees, výkon, statistiky per zápas
Description: Přiřazuje rozhodčí k zápasům a trackuje jejich výkon: počet karet,
  fauly, kontroverze. Aktualizuje Referees záznam a MatchStats.RefereeGuid.
Inputs: MatchGuid, referee name, performance data (cards given, fouls called)
Outputs: Referees Guid (upsert), MatchStats.RefereeGuid update, performance metrics
Limitations: rozhodčí matching přes match-upsert-guardian; nehodnotí subjektivně

## var-event-recorder
Task types: VAR, video assistant referee, VAR decisions
Description: Zaznamenává VAR rozhodnutí jako MatchEvents záznamy (EventType = VAR).
  Typy: goal_disallowed, penalty_awarded, red_card_review, mistaken_identity.
Inputs: MatchGuid, VAR decision type, minute, outcome (overturned/upheld)
Outputs: MatchEvents záznam (VAR type), Metadata s rozhodnutím
Limitations: pouze ligy s VAR; bez VAR v competition konfiguraci → skip

## extra-time-handler
Task types: prodloužení, extra time, penalty shootout trigger
Description: Zpracovává výsledky prodloužení: aktualizuje MatchStatus, skóre po 90 min
  vs po ET. Detekuje nutnost penalty shootout (remíza po ET).
Inputs: MatchGuid, score_90, score_et, has_extra_time flag
Outputs: aktualizovaný Match (ET skóre, status), penalty shootout trigger flag
Limitations: pouze pro eliminační zápasy; ligové zápasy extra time neřeší

## penalty-shootout-recorder
Task types: penalty shootout, PK výsledky, shootout záznamy
Description: Zaznamenává výsledky penalty shootoutu: pořadí hráčů, proměněné/neproměněné,
  celkové skóre PK. Nastavuje WinnerTeamGuid dle PK výsledku.
Inputs: MatchGuid, shootout sequence (player, team, scored/missed)
Outputs: MatchEvents záznamy (penalty_scored/missed), WinnerTeamGuid update
Limitations: pouze po prodloužení v eliminačním zápase; bez ET context → error

## substitution-event-processor
Task types: střídání, Substitution, 2 MatchEvents per střídání
Description: Zpracovává střídání jako 2 MatchEvents záznamy: EventType=sub_in (vstupující)
  + EventType=sub_out (vystupující). Aktualizuje MinutesPlayed v MatchLineups.
Inputs: MatchGuid, minute, player_in, player_out, team
Outputs: 2 MatchEvents záznamy, aktualizovaný MinutesPlayed pro oba hráče
Limitations: max 5 střídání per tým (standardní fotbal); rozšíření dle competition config

## injury-time-calculator
Task types: nastavený čas, injury time, added time
Description: Zaznamenává přidaný čas (injury time) per poločas do Matches.Metadata.
  Počítá efektivní délku hry pro analytické účely.
Inputs: MatchGuid, added_time_first_half, added_time_second_half, added_time_et
Outputs: Matches.Metadata update s injury time, effective_match_duration výpočet
Limitations: injury time je přibližná hodnota; variabilita dle zdroje

## match-rating-aggregator
Task types: player ratings, match ratings, hodnocení výkonu
Description: Agreguje hodnocení výkonu hráčů per zápas ze zdrojů (WhoScored, SofaScore).
  Ukládá do MatchLineups.Rating nebo PlayerSeasonStats. Průměr z více zdrojů.
Inputs: MatchGuid, source, player ratings list (PlayerGuid, rating 1-10)
Outputs: MatchLineups.Rating update, PlayerSeasonStats update, multi-source average
Limitations: rating scale normalizuje na 0-10; min 1 zdroj; průměr max ze 3 zdrojů

## matchday-summary-generator
Task types: matchday summary, souhrn kola, ntfy zpráva
Description: Generuje souhrn po zpracování všech zápasů matchday kola:
  počet zápasů, góly, karty, tabulka top scorers, biggest wins. Posílá na ntfy.
Inputs: CompetitionGuid, SeasonGuid, matchday number, zpracované MatchGuids
Outputs: matchday summary text, ntfy notifikace na agent-runs
Limitations: jedna zpráva per matchday; sdílí ntfy limit (max 3/run)

-- H. RUN ORCHESTRATION (8) ------------------------------------

## scheduled-run-report-formatter
Task types: finální run report, formátování výstupu
Description: Generuje strukturovaný run report ve standardním formátu ManagerPrompt.
  Zahrnuje: startup sekci, zpracované zápasy per datum, Post-Match Pipeline výsledky
  (Krok 1-11), errors, highlights, SkillsUsed[], capability_missing[], duration.
Inputs: raw run data (matches processed per date, pipeline results, errors, highlights, SkillsUsed)
Outputs: formátovaný run report připravený pro upsert_record("AgentRunReports", ...)
Limitations: report musí být uložen i při selhání runu

## fallback-run-auditor
Task types: fallback-mode evaluace, degraded run assessment
Description: Vyhodnocuje run v degraded módu (DB nedostupný, zdroje blokované,
  time budget překročen). Rozhoduje, které kroky Post-Match Pipeline lze bezpečně
  odložit (Tier 2/3) a co musí být zachováno jako minimum (Tier 1 + run report).
Inputs: aktuální stav DB, circuit breakerů, zbývající time budget
Outputs: fallback execution plan, odložené kroky s tier klasifikací, doporučení pro next run
Limitations: nelze použít pokud je DB zcela nedostupná (db_unreachable = STOP)

## sheet-fallback-mapper
Task types: export do Google Sheets, fallback při nedostupnosti DB
Description: Exportuje sebraná data zápasů do Google Sheets jako fallback zálohu
  při dočasné nedostupnosti DB. Data jsou strukturována pro pozdější reimport.
Inputs: sebraná data zápasů (Matches, MatchEvents, MatchStats...), cílový spreadsheet ID
Outputs: Google Sheets export s metadaty (timestamp, source, agent, run ID, datum zápasů)
Limitations: pouze fallback — pokud DB dostupná, vždy preferovat přímý zápis do DB

## next-run-calendar-scheduler
Task types: plánování příštího runu v Google Calendar, AI Managers
Description: Vytváří Calendar event pro příští scheduled run. CALENDAR_ID je pevně nastaven:
  AgentType=Manager: "a43e278e3a817484c52c5fc24439845d6aa230c2280ca25eb2a08bae2e5f4eef@group.calendar.google.com"
  NIKDY nepoužívat řetězec "AI Managers" přímo jako calendar_id parametr.
  Před vytvořením: list_events → smazat starý event se stejným AgentName → vytvořit nový.
  Zajišťuje, že existuje právě JEDNA budoucí událost pro agenta.
  Při úspěšném runu suffix " ✅" + počet zpracovaných zápasů, při selhání " ❌".
Inputs: agent name, výsledek runu, počet zpracovaných zápasů, čas příštího scheduled runu (NextRunAt)
Outputs: Calendar event vytvořen s pevným CALENDAR_ID, potvrzení
Limitations: CALENDAR_ID je hardcoded — list_calendars() není potřeba; nemaže historické eventy (jen budoucí duplicity)

## time-budget-governor
Task types: time budget, graceful stop, run timeout
Description: Sleduje zbývající time budget (8 min scheduled / 15 min manual).
  Při dosažení 80% budgetu spustí graceful stop: dokončí aktuální krok, přeskočí zbývající.
Inputs: start time, run type (scheduled/manual), current step
Outputs: zbývající čas, allow/stop decision, warning při 80%
Limitations: Tier1 pipeline kroky NIKDY nepřeruší; čeká na jejich dokončení

## missed-run-catchup-handler
Task types: missed run, catch-up mode, historické zápasy
Description: Detekuje výpadky runů. Gap > 2x interval → catch-up mode pro historické
  zápasy max 3 dny zpět. Prioritizuje Tier1 data pro dohnané zápasy.
Inputs: AgentName, expected_interval, LastRunAt timestamp
Outputs: catch-up plán (datum od-do, Tier priority), ntfy notifikace
Limitations: max 3 dny zpět; starší zápasy flaguje jako low_priority

## run-checkpoint-manager
Task types: checkpoints, resume after interruption, pipeline state
Description: Ukládá stav post-match pipeline na checkpointech (po každém zpracovaném zápasu).
  Umožňuje pokračování po přerušení bez opakování dokončených zápasů.
Inputs: run ID, processed matches list, pending matches, pipeline state
Outputs: checkpoint uložen, resume point pro restart
Limitations: checkpoint do AgentRunReports Metadata; max 24h platnost

## circuit-breaker-coordinator
Task types: circuit breaker evaluation, run start check, source availability
Description: Evaluuje stav všech circuit breakerů na začátku runu. Sestavuje seznam
  dostupných a nedostupných zdrojů (Flashscore, API, scraping targets).
Inputs: seznam zdrojů používaných agentem
Outputs: available sources, blocked sources s důvodem, fallback doporučení
Limitations: nerestartuje circuit breakery; pouze evaluuje stav

-- I. IMAGE & MEDIA (9) ----------------------------------------

## vo2qnapdb-image-guardian
Task types: image workflow, SharedImages + EntityImages, QNAP upload
Description: Kompletní image workflow: validace (MIME, velikost, hash, bezpečný filename),
  dedup přes SHA256, upload přes save_image_url nebo save_image_base64,
  zápis do SharedImages + EntityImages s historizací (IsCurrent, ValidFrom/ValidTo).
  Pro sport: loga klubů, portréty hráčů/trenérů, venue fotografie.
Inputs: URL nebo base64 obrázku, EntityGuid, EntityType, ImageType
Outputs: SharedImages Guid, EntityImages záznam, upload potvrzení
Limitations: pouze když explicitně vyžadováno; SVG bez scriptů; povolené MIME: png, webp, jpeg, svg+xml

## club-logo-updater
Task types: logo klubu, Teams.LogoUrl, EntityImages
Description: Vyhledává, validuje a uploaduje logo klubu. Aktualizuje Teams.LogoUrl
  a vytváří EntityImages záznam (ImageType=logo). Respektuje IsCurrent historii.
Inputs: TeamGuid, logo URL nebo base64
Outputs: SharedImages Guid, EntityImages záznam, Teams.LogoUrl update
Limitations: pouze explicitní volání; automatický update loga nepovoluje bez souhlasu

## player-portrait-manager
Task types: fotografie hráče, Players.PhotoUrl, EntityImages
Description: Spravuje portréty hráčů: upload headshot fotografie, aktualizace
  Players.PhotoUrl, historizace starých fotografií (IsCurrent timeline).
Inputs: PlayerGuid, photo URL nebo base64, season (volitelně pro kontext)
Outputs: SharedImages Guid, EntityImages záznam, Players.PhotoUrl update
Limitations: pouze frontální portrét; group fotografie neakceptuje

## venue-photo-uploader
Task types: fotografie stadionu, Venues, EntityImages
Description: Uploaduje fotografie stadionů a tréninkových areálů. Asociuje s Venues
  záznamy. Podporuje multiple foto per venue (ImageType: exterior/interior/aerial).
Inputs: VenueGuid, photo URL nebo base64, photo type (exterior/interior/aerial)
Outputs: SharedImages Guid, EntityImages záznam, potvrzení
Limitations: max 5 fotografií per venue; pouze schválené veřejné fotografie

## historical-asset-collector
Task types: historické loga, historické cresty, historické dresy, historical timeline
Description: Sbírá VŠECHNY historické verze vizuálních assetů pro sportovní entitu.
  Buduje kompletní timeline od vzniku klubu/soutěže — loga, cresty, vlajky.
  Každá verze ukládána s from/to metadata a ConfidenceScore dle jistoty datování.
  Zdroje: Wikidata P154/P18/P1419, Wikipedia Commons, sportslogos.net.
Inputs: EntityGuid, EntityType (Teams/Competitions/Countries), ImageTypes[], max_age_years
Outputs: seznam historických assetů s from/to/circa metadata, EntityImages záznamy
Limitations: datování může být přibližné (from_circa=true); max 50 historických assetů per run

## wikidata-image-harvester
Task types: Wikidata P154, P18, P1419, structured image metadata
Description: Dotazuje Wikidata API pro vizuální assety entity — logo (P154), image (P18),
  coat_of_arms (P1419). Extrahuje všechny historické verze s přesnými daty redesignů.
  Mapuje na správný ImageType enum (logo_primary, crest_historical, flag_national...).
Inputs: WikidataId (Q-identifier) nebo EntityGuid s existujícím WikidataId
Outputs: seznam image URL s ImageType, from/to datováním, zdroj Commons
Limitations: vyžaduje WikidataId na entitě; SPARQL timeout pro velké queries

## kit-timeline-builder
Task types: historické dresy, kit per sezóna, kitsdb.com, historicalkits.co.uk
Description: Sbírá historické dresy (kit_home, kit_away) pro tým per sezóna od ~1970.
  Primární zdroj: kitsdb.com, historicalkits.co.uk, Wikipedia Commons.
  Ukládá každý kit s ValidFrom=SeasonStart, ValidTo=SeasonEnd, IsCurrent dle aktuálnosti.
  Metadata: {from: "2023", to: "2024", kit_type: "home", sponsor: "Nike", colors: "#004C97"}
Inputs: TeamGuid, season_range (volitelně: "2000-2024")
Outputs: EntityImages záznamy per sezóna/kit_type, timeline přehled
Limitations: kitsdb.com pokrývá ~1970+; starší dresy s from_circa=true

## season-logo-collector
Task types: season_logo, season_badge, season_artwork, UCL, Premier League per sezóna
Description: Sbírá season-specific loga pro sledované soutěže. Každá sezóna může mít
  unikátní branding (UCL 2023/24 vs 2024/25). EntityType="Seasons", IsCurrent pro probíhající.
  Zdroje: official competition website, Wikipedia Commons, UEFA/FIFA media.
Inputs: CompetitionGuid, season_range (volitelně)
Outputs: EntityImages záznamy per sezóna (season_logo, season_badge, season_artwork)
Limitations: starší sezóny mohou mít nižší ConfidenceScore; max 20 sezón per run

## image-quality-validator
Task types: rozlišení, velikost, formát, min quality check
Description: Ověřuje kvalitu obrázku před uložením. Kontroluje minimální rozlišení
  (100×100px ikony, 200×200px loga, 500×500px cresty/vlajky), preferenci SVG pro
  vektorová loga, max 15 MB. Flaguje obrázky pod minimem.
Inputs: image URL nebo base64, ImageType (pro určení min rozlišení)
Outputs: pass/fail, detected dimensions, file size, doporučení (prefer SVG)
Limitations: SVG rozlišení nelze měřit jako raster; animované GIF pouze základní check

-- J. ENRICHMENT & BACKFILL (8) --------------------------------

## relationship-backfill-enricher
Task types: backfill chybějících relací, FK enrichment, sport data
Description: Doplňuje chybějící FK vazby v sportovních tabulkách: WinnerTeamGuid,
  RefereeGuid, VenueGuid. Pořadí: NULL first (IS NULL DESC), pak nejstarší UpdatedAt ASC.
  Sloupce mohou být přepsány; nikdy nepřepisuje hodnotou NULL.
Inputs: tabulka, FK sloupce k doplnění, max count (default 100)
Outputs: počet doplněných záznamů, přeskočené (zdroj nedostupný)
Limitations: max 100 per tabulka per run

## null-field-enricher
Task types: NULL pole, sport data backfill, priority queue
Description: Identifikuje NULL hodnoty v prioritních sport sloupcích (Score 90+: HomeScore,
  AwayScore, Status, StartTimeUtc) a doplňuje ze zdrojů.
Inputs: tabulka, target columns, max count
Outputs: počet doplněných hodnot, zdroj per value
Limitations: max 100 per tabulka per run; neopravuje hodnotou NULL

## confidence-decay-manager
Task types: confidence decay, -5 bodů / 30 dní, sport data freshness
Description: Aplikuje confidence decay na sportovní data: historické zápasy starší 90 dní
  bez aktualizace ztratí -5 ConfidenceScore per 30 dní. Min = 10.
Inputs: seznam tabulek (Matches, Players, Teams...), datum výpočtu
Outputs: počet aktualizovaných záznamů, průměrný score před/po
Limitations: decay pouze na záznamy starší 30 dní bez UpdatedAt update

## external-id-mapper
Task types: ExternalID, Flashscore ID, Sofascore ID, cross-referencing
Description: Mapuje ExternalID ze sportovních zdrojů (Flashscore, Sofascore, WhoScored)
  na interní Guidy. Udržuje obousměrný index pro rychlé matching.
Inputs: ExternalID, source system (flashscore/sofascore/whoscored), EntityType
Outputs: mapování ExternalID → Guid, nový záznam pokud neexistuje
Limitations: jeden ExternalID per source system per entita

## source-reliability-tracker
Task types: spolehlivost sportovních zdrojů, SourceReliability
Description: Sleduje success/failure ratio per sportovní zdroj. Aktualizuje
  ReliabilityScore v SourceReliability. Flaguje nespolehlivé zdroje.
Inputs: source name (flashscore/sofascore...), výsledek requestu, response time
Outputs: aktualizovaný ReliabilityScore, trend
Limitations: min 10 requestů pro výpočet; nový zdroj začíná na 50

## entity-alias-registry
Task types: aliasy, alternativní názvy týmů/soutěží/hráčů
Description: Udržuje aliasy pro sportovní entity: zkrácené názvy týmů, historické
  názvy klubů, alternativní pravopis jmen hráčů.
Inputs: EntityGuid, alias, alias type (short_name/historical/alternate_spelling)
Outputs: alias záznam, aktualizovaný alias index
Limitations: max 50 aliasů per entita; duplicitní aliasy ignoruje

## cross-source-verifier
Task types: cross-source validace, score konzistence, sport data agreement
Description: Porovnává výsledky zápasů napříč zdroji (Flashscore vs Sofascore vs API).
  Flaguje diskrepance v HomeScore/AwayScore jako HIGH PRIORITY pro review.
Inputs: MatchGuid, score values per source
Outputs: agreement/disagreement, flagované diskrepance, canonical value doporučení
Limitations: min 2 zdroje; diskrepance v score vždy → ManualReviewQueue

## data-quarantine-manager
Task types: karanténa, podezřelá sportovní data, QuarantineQueue
Description: Přesouvá podezřelá sportovní data do QuarantineQueue: duplicitní zápasy,
  anomální skóre, neidentifikované týmy. Informuje přes ntfy agent-maintenance.
Inputs: podezřelý záznam, reason type, source
Outputs: QuarantineQueue záznam, ntfy notifikace
Limitations: max 200 záznamů v queue; nepřepisuje produkční data

-- K. TRANSFER & INJURY (8) ------------------------------------

## transfer-window-monitor
Task types: Transfer Window, IsCurrentlyOpen, squad governance
Description: Monitoruje stav Transfer Window per competition. Aktualizuje
  TransferWindow.IsCurrentlyOpen flag dle dat. Blokuje neoprávněné squad changes.
Inputs: CompetitionGuid, aktuální datum
Outputs: IsCurrentlyOpen status (true/false), window dates, next window start
Limitations: window konfigurace musí být předem uložena; neodhazuje bez dat

## transfer-record-processor
Task types: přestupové záznamy, Transfers tabulka, fee, loan
Description: Vytváří a aktualizuje záznamy v Transfers tabulce: permanent transfer,
  loan, free agent, end of loan. Ukládá fee (v EUR) pokud dostupné.
Inputs: PlayerGuid, from/to TeamGuid, transfer_type, date, fee (volitelně)
Outputs: Transfers záznam, squad update trigger
Limitations: historické transfery pouze; fee NULL pokud nedostupné nebo undisclosed

## injury-report-parser
Task types: zranění, PlayerUnavailability, injury type classification
Description: Parsuje zprávy o zraněních hráčů ze zdrojů. Klasifikuje injury type
  (muscle/bone/ligament/illness/other). Vytváří PlayerUnavailability záznamy.
Inputs: raw injury report text nebo structured data, PlayerGuid
Outputs: PlayerUnavailability záznam (type, start_date, expected_return)
Limitations: expected_return je odhad ze zdroje; mění se; uchovává historii změn

## unavailability-period-calculator
Task types: délka absence, PlayerUnavailability, zápasy vynechané
Description: Vypočítává délku absence hráče v zápasech (kolik ligových zápasů vynechá).
  Aktualizuje PlayerUnavailability.MissedMatches počet.
Inputs: PlayerUnavailability Guid, CompetitionGuid, start_date, expected_end
Outputs: estimated missed matches count, seznam vynechaných MatchGuidů
Limitations: pouze plánované zápasy; postponed zápasy se odečítají

## return-date-estimator
Task types: návrat hráče, return date update, recovery tracking
Description: Aktualizuje expected return date v PlayerUnavailability na základě
  nových informací ze zdrojů. Loguje historii změn do Metadata.
Inputs: PlayerUnavailability Guid, nový expected_end_date, source, confidence
Outputs: aktualizovaný PlayerUnavailability záznam, change history v Metadata
Limitations: neguessuje bez zdroje; nepotvrzené návraty označuje jako tentative

## suspension-period-calculator
Task types: délka suspenze, accumulated cards, ban calculation
Description: Počítá délku disciplinárního zákazu dle akumulovaných karet a
  competition pravidel. Vytváří PlayerUnavailability záznam (reason=suspension).
Inputs: PlayerGuid, CompetitionGuid, suspension trigger (cards/direct red/misconduct)
Outputs: ban length (matches), PlayerUnavailability záznam, games_to_serve
Limitations: délka banu dle Competition.TiebreakerJson pravidel; bez pravidel default 1

## loan-deal-tracker
Task types: hostování, loan, temporary transfers, Squads
Description: Trackuje dočasné přestupy (loan deals): vytváří Transfers záznam
  (transfer_type=loan), aktualizuje Squads pro oba týmy, ukládá recall clause.
Inputs: PlayerGuid, loan_from TeamGuid, loan_to TeamGuid, loan_start, loan_end
Outputs: Transfers záznam (loan), Squads záznamy oba týmy, recall flag
Limitations: jeden aktivní loan per hráč; nested loans nepovoluje

## contract-expiry-monitor
Task types: smlouvy, expiry, free agent status, squad governance
Description: Sleduje blížící se konce smluv hráčů (contract_expiry v Squads.Metadata).
  Flaguje hráče jejichž smlouva vyprší do 6 měsíců pro monitoring.
Inputs: TeamGuid, SeasonGuid, look-ahead period (default 6 měsíců)
Outputs: seznam hráčů s blížícím se koncem smlouvy, days_remaining, free_agent_flag
Limitations: contract_expiry musí být uložen v Metadata; bez dat nelze sledovat

-- L. NOTIFICATION & REPORTING (5) ----------------------------

## ntfy-notification-sender
Task types: ntfy notifikace, agent-runs, agent-errors, agent-alerts, agent-maintenance, agent-digest
Description: Odesílá notifikace na ntfy.vo2info.cz pro sportovního agenta.
  MCP tool: send_notification — VŽDY z VO2QNAPDB MCP konektoru, NIKDY přímý HTTP na ntfy ani JobQueue:
    Manager/TE agenti → VO2QNAPDBTE konektor
    MAB agenti        → VO2QNAPDBMAB konektor
    USM agenti        → VO2QNAPDBUSM konektor
  Pokud tool v konektoru nevidíš: refreshni konektor v ChatGPT (odpoj + připoj znovu).
  NIKDY nenahrazovat přes JobQueue ani enqueue_job — JobQueue je pro datové úlohy, ne notifikace.
  Pokud send_notification po refreshi stále chybí: log capability_missing("send_notification"), pokračovat.
  agent-runs: výsledky runů, výsledky zápasů, standings. agent-errors: POUZE kritické bloky (priority urgent).
  agent-alerts: varování (source blocked, odds integrity flag, missed run, circuit breaker).
  agent-maintenance: backfill, season lifecycle, hráčské disciplíny. agent-digest: týdenní/měsíční souhrny.
  Sport emoji dle sportu: fotbal=soccer, basketbal=basketball, hokej=ice_hockey.
  Max 3 notifikace per run (digest se nepočítá do limitu).
  Jazyk zpráv: česky.
  FORMÁT — přesná šablona (žádný JSON dump, žádné slovníky v message):
    title:   max 60 znaků — začíná emoji stavem: ✅ ⚠️ 🚨 🔧
    message: max 200 znaků — pipe-separated hodnoty: "Klíč: hodnota | Klíč: hodnota"
    tags:    ntfy tag slova v poli — white_check_mark | warning | rotating_light | wrench + sport tag
    Příklady:
      ✅ title: "✅ Football Data Manager — run OK"
         message: "Zápasy: 24 | Nové: 8 | Aktu: 16 | Doba: 5m 12s"
         tags: ["white_check_mark", "soccer"]
      ⚠️ title: "⚠️ Football Data Manager — run selhal"
         message: "Blokér: db_error | Zprac.: 3/24 | Chyba: connection timeout"
         tags: ["warning", "soccer"]
      🚨 title: "🚨 Football Data Manager — kritická chyba"
         message: "DB: unreachable | Akce: run zastaven"
         tags: ["rotating_light", "soccer"]
Inputs: kanál, zpráva, sport emoji tag, priority (default/high/urgent)
Outputs: notifikace odeslána, message ID
Limitations: max 3 ntfy per run; překročení agreguje do jedné souhrnné zprávy; dostupnost závisí na MCP session

## audit-log-writer
Task types: AuditLog, change tracking, sport data changes
Description: Zapisuje strukturované záznamy do AuditLog pro všechny změny sportovních dat.
  Prioritizuje: Matches výsledky, Standings změny, Transfer záznamy.
Inputs: action type, EntityGuid, table, before/after hodnoty, actor
Outputs: AuditLog záznam Guid
Limitations: before/after max 10 KB per záznam; SELECT operace neloguje

## error-escalation-manager
Task types: error escalation, ntfy agent-alerts, Tier3 sport errors
Description: Eskaluje Tier3 chyby sport agenta na ntfy agent-alerts. Tier4 (kritické bloky —
  db_unreachable, run zastaven) eskaluje na agent-errors. Kontextualizuje zprávu pro sport:
  jaký zápas/soutěž/datum selhal a proč.
Inputs: Tier3/4 error list, AgentName, run ID, sport context
Outputs: ntfy zprávy odeslány (agent-alerts nebo agent-errors), escalation log
Limitations: max 3 ntfy per run; sdílí limit s dalšími skills

## compliance-audit-generator
Task types: governance compliance, ManagerPrompt governance, sport data audit
Description: Generuje compliance report: Tier1 vždy zpracován, SCOPE (pouze historické
  zápasy), soft-delete policy, max 100 backfill/tabulka, run report vždy uložen.
Inputs: run data, AgentName, časové rozmezí
Outputs: compliance score (0-100), violations, doporučení
Limitations: detekuje porušení trackovatelná v DB a AgentRunReports

## run-status-broadcaster
Task types: run status, agent-runs kanál, sport run summary
Description: Odesílá standardizovanou zprávu po dokončení runu na agent-runs.
  Sport formát: [AgentName] [OK/WARN/ERROR] | zápasy:N | Tier1:OK | duration:Xs
Inputs: run výsledek, processed matches count, Tier1/2/3 status, duration
Outputs: ntfy zpráva na agent-runs
Limitations: jedna zpráva per run; v limitu 3 ntfy/run

-- M. VIEWS & ANALYTICS (5) ------------------------------------

## analytic-views-refresher
Task types: CREATE OR REPLACE views, analytické views, standings views
Description: Obnovuje všechny analytické views po runu: standings view, top_scorers view,
  form_table view, H2H summary. Používá CREATE OR REPLACE pro bezpečný rebuild.
Inputs: seznam views k obnovení (nebo ALL)
Outputs: počet obnovených views, failed views s důvodem
Limitations: pouze CREATE OR REPLACE; DROP VIEW nepovoluje

## form-trend-analyzer
Task types: trend analýza, TeamForm, PlayerForm, improving/declining
Description: Analyzuje trend formy (improving/stable/declining) na základě rolling stats.
  Srovnává posledních 5 zápasů vs předchozích 5. Aktualizuje TrendScore.
Inputs: EntityGuid (tým nebo hráč), look-back period
Outputs: trend klasifikace (improving/stable/declining), TrendScore delta
Limitations: min 10 zápasů pro trend analýzu; méně = insufficient data

## season-statistics-compiler
Task types: sezónní statistiky, agregace, competition summary
Description: Kompiluje celkové statistiky za sezónu: celkem gólů, průměr gólů na zápas,
  home/away split, disciplinární statistiky, attendance průměr.
Inputs: CompetitionGuid, SeasonGuid
Outputs: season_summary JSON, celkové statistiky per competition
Limitations: pouze pro ukončené nebo probíhající sezóny s dostatkem dat (min 50%)

## historical-data-auditor
Task types: historická data, data completeness, archivní audit
Description: Audituje historická data per competition/season: completeness zápasů,
  Tier1/2/3 data coverage, missing periods. Generuje historický coverage report.
Inputs: CompetitionGuid, SeasonGuid range (od-do)
Outputs: historical audit report (coverage %, missing periods, data quality score)
Limitations: max 5 sezón najednou; starší data mohou mít legitimně nižší coverage

## performance-benchmark-tracker
Task types: benchmark výkonu agenta, run metrics, SLA tracking
Description: Trackuje výkonnostní metriky agenta across runů: zápasů per run, duration,
  error rate, coverage growth. Srovnává s baseline (první 30 dní runu).
Inputs: AgentName, look-back period (default 30 dní)
Outputs: performance trend (improving/stable/degrading), SLA compliance per metric
Limitations: vyžaduje min 10 runů pro benchmark; nové agenty mají insufficient data period

-- N. ADVANCED OPERATIONS (12) -----------------------------------

## manager-baseline-file-manager
Task types: baseline soubor agenta, tab 'entities' v master spreadsheet, RowCount, LastPopulated aktualizace, bootstrap
Description: Čte a aktualizuje tab 'entities' v master spreadsheet {AgentName} v Managers složce
  (folder ID: 1lEvffJ-rjdExCwMWxmWM7PchnKkGTGUO). Načítá přehled sportovních tabulek
  (ImportantScore, RowCount, LastPopulated) na startu pro prioritizaci. Aktualizuje na konci runu.
  Formát řádku: ImportantScore | TableName | Description | RowCount | LastPopulated
  Hledání: search_files(query="{AgentName}", folderId="1lEvffJ-rjdExCwMWxmWM7PchnKkGTGUO")
  Čtení: get_spreadsheet_range(spreadsheetId, range="entities!A:Z")
  Zápis: sheets_append_rows(spreadsheetId, range="entities", rows=[...])
  BOOTSTRAP: pokud master spreadsheet neexistuje → vytvořit automaticky:
    1. SELECT tablename, COUNT(*) FROM pg_tables JOIN ... pro všechny public tabulky
    2. Každá tabulka dostane ImportantScore=50 (default), RowCount z COUNT(*), LastPopulated=now()
    3. sheets_create_spreadsheet(title="{AgentName}", folderId="1lEvffJ-rjdExCwMWxmWM7PchnKkGTGUO")
    4. batch_update_spreadsheet → 4 taby (entities, names, urls, error) + append headers
    5. Pokud create selže → capability_missing("sheets_create_spreadsheet"), pokračovat degraded (ne blocked)
Inputs: AgentName, seznam tabulek s aktuálním RowCount a LastPopulated ze SELECT COUNT(*)
Outputs: přečtený katalog (startup), aktualizovaný tab 'entities' zapsaný na Drive (end of run), bootstrap log
Limitations: neaktualizuje pokud db_status = db_unreachable; DryRun = nezapisuje; bootstrap vyžaduje DB přístup pro COUNT(*)

## dry-run-mode-controller
Task types: DryRun mode, testovací beh bez DB zápisů, simulace
Description: Řídí DryRun mod pro sport agenty: blokuje INSERT/UPDATE/DELETE, simuluje výstupy
  s "[DRY_RUN]" prefixem. Calendar eventy se netvoří. Ntfy odesílá s "[DRY_RUN TEST]".
  upsert_record("AgentRunReports", ...) se provede s IsDryRun=true.
Inputs: RunMode: dry_run nebo DryRunAllowed: true + dry_run=true parametr
Outputs: simulované výstupy, run report s IsDryRun=true, závěrečná zpráva "DRY RUN COMPLETE"
Limitations: DB čtení povoleno; Drive čtení povoleno; veškeré zápisy (DB/Drive/Calendar) blokovány

## match-postponement-handler
Task types: odložení zápasu, zrušení, Status=postponed/cancelled/rescheduled
Description: Zpracovává odložení a zrušení zápasů: nastavuje Status=postponed nebo cancelled,
  ukládá OriginalDate, PostponementReason/CancellationReason. Vytváří MatchEvents
  (postponement_announced). Náhradní zápas = NOVÝ Matches záznam s RescheduledFromMatchGuid.
Inputs: MatchGuid, nový status (postponed/cancelled), reason, nové datum (pokud known)
Outputs: aktualizovaný Matches záznam, MatchEvents záznam, nový náhradní Matches (při reschedule)
Limitations: NESMAZAT původní záznam; NEZAPOČÍTÁVAT postponed do standings/form/coverage

## international-calendar-manager
Task types: InternationalCalendar, FIFA/UEFA přestávky, match collection scheduling
Description: Spravuje InternationalCalendar záznamy: FIFA/UEFA přestávky, affected leagues.
  Při aktivní přestávce: domácí ligové zápasy nepočítá. Pokud Matches count = 0 pro 7+ dní
  aktivní ligy → kontrola přestávky. Plánuje Calendar event pro mezinárodní okno.
Inputs: WindowType (fifa_international/uefa_nations_league/euro_qualifying/...), StartDate, EndDate, AffectedLeagues
Outputs: InternationalCalendar záznamy, data error flag pokud zápas detekován v přestávce
Limitations: scheduling match collection pouze mimo okno přestávky; bez konfigurace nelze detekovat

## sport-date-coverage-manager
Task types: SportDateCoverage, exhausted datumy, automatické znovuotevření, verification status
Description: Spravuje SportDateCoverage: označuje exhausted (po 5 runech s multi-source verifikací),
  automaticky znovu otevírá datumy starší 1 roku (IsExhausted=false, IsPendingRecheck=true).
  Řídí VerificationStatus přechody (pending → evidence_found → exhausted).
Inputs: SportGuid, TargetDate, výsledek vyhledávání, sources checked, NoMatchCount
Outputs: aktualizovaný SportDateCoverage záznam, exhausted/reopen/pending_recheck rozhodnutí
Limitations: vcerejsek NIKDY neoznacovat jako exhausted; exhaust pouze po 5 runech + 2 nezávislé zdroje

## match-preview-generator
Task types: Match Preview, H2H, forma, nedostupní hráci, ntfy/Calendar description
Description: Generuje pre-match preview ze 100% historických DB dat: H2H posledních 5 zápasů,
  forma obou týmů (last 5+10 z TeamForm), nedostupní hráci (PlayerUnavailability, PlayerDiscipline),
  standings, odds trendy. Výstup jako ntfy nebo Calendar description.
Inputs: HomeTeamGuid, AwayTeamGuid, MatchDate, CompetitionGuid
Outputs: formátovaná Preview zpráva, limited_data flag pokud H2H < 2 zápasy
Limitations: max 24h před zápasem; NIKDY nesebírá live/pre-match data; neukládá jako DB záznam

## player-career-manager
Task types: PlayerCareer, kariéra hráče, angažmá napříč kluby, career aggregation
Description: Spravuje PlayerCareer tabulku (jedno angažmá = jeden řádek): vytváří záznamy
  při přestupu (JoinedDate, TransferType), nastavuje LeftDate + IsCurrentClub=false při odchodu.
  Agreguje AppearancesTotal, GoalsScored, Assists z PlayerSeasonStats.
Inputs: PlayerGuid, TeamGuid, TransferType, JoinedDate, LeftDate (volitelně), kompetice
Outputs: PlayerCareer záznamy (upsert), IsCurrentClub flag aktualizace, career aggregace
Limitations: max 1 IsCurrentClub=true per PlayerGuid; půjčka (loan_in) = vlastní PlayerCareer řádek

## weekly-digest-reporter
Task types: weekly/monthly digest, sport souhrn, Drive /Prompts/Reports/
Description: Generuje týdenní (každých 7 runů nebo každé pondělí) a měsíční (každých 30 dní)
  digest pro sport agenty: přehled zpracovaných zápasů, coverage progress, errors across runů,
  cross-agent tasks přidělené/zpracované. Ukládá na Drive, odesílá ntfy na agent-maintenance.
Inputs: AgentName, period type (weekly/monthly), data z get_agent_run_reports look-back
Outputs: digest soubor na Drive /Prompts/Reports/{AgentName}/, ntfy summary na agent-maintenance
Limitations: min 3 runy pro smysluplný weekly digest; bez dat reportuje "insufficient_data"

## anti-bot-fallback-handler
Task types: anti-bot detekce, 403/429/503, fallback zdroj, sport-specifické scraping
Description: Detekuje anti-bot signály (HTTP 403, 429, 503, CAPTCHA, prázdný body s 200,
  redirect na /challenge). Přepíná na secondary → tertiary zdroj. Nikdy neobchází CAPTCHA.
  Neretryuje blokovanou URL více než 2× za run.
Inputs: failed URL, HTTP status code, response body indicator, backup source list
Outputs: selected fallback source, circuit breaker update, ntfy high-priority alert
Limitations: Selenium fallback jen pokud direct HTTP blokován + Selenium endpoint dostupný; max 10 page loadů

## cross-agent-task-writer
Task types: cross-agent zápis, tab 'names', tab 'urls', task assignment do Catalog agentů
Description: Zapisuje úkoly pro Catalog agenty do jejich master spreadsheet {AgentName} (append-only).
  Manager agent koordinuje Catalog agenty přes tuto skill. Max 50 položek per zápis.
  Hledání: search_files(query="{cílový AgentName}", folderId="{agentTypeFolderId}")
  Zápis: sheets_append_rows(spreadsheetId, range="names", rows=[...]) nebo range="urls"
  Zaznamenává operaci v highlights[]: "Cross-agent task: N items written to {AgentName} tab 'names'".
Inputs: cílový CatalogAgentName, seznam názvů (tab 'names') nebo URL (tab 'urls'), typ (names/urls)
Outputs: append do příslušného tabu master spreadsheet, highlights[] záznam s počtem zapsaných položek
Limitations: nesmí přepisovat celý tab; max 50 položek per zápis; tab max 500 řádků

## agent-schedule-manager
Task types: AgentSchedules, denní čas spuštění, přehled agentů, přehled plánů (conflict detection zakázána)
Description: Spravuje tabulku AgentSchedules — ukládá denní plánovaný čas spuštění každého agenta.
  Na konci každého runu provede UPSERT (AgentName, ScheduledRunTime, NextRunAt, LastRunAt).
  Na startu načte přehled všech aktivních agentů (paralelní běh více agentů je povolený).
Inputs: AgentName, ScheduledRunTime (time, Europe/Prague), IntervalHours, AgentType (Catalog/Manager)
Outputs: upsert do AgentSchedules, přehled tabulky agentů
Limitations: čas v Europe/Prague; NEZDVOJIT upsert při DryRun

## problem-report-writer
Task types: problem report, tab 'error' v master spreadsheet, nedokončený run, chyby agenta
Description: Zapíše problem report do tabu 'error' v master spreadsheet {AgentName}
  pokud run skončil s success=false nebo selhal ntfy/Calendar event/DB report.
  Tab 'error' obsahuje: MCP identifikátor (VO2QNAPDBAI/VO2QNAPDBTE/VO2QNAPDBMAB/VO2QNAPDBUSM),
  problémy ve formátu "ProblemScore | Category | Description | Timestamp | StepStatus" (seřazeno sestupně dle ProblemScore),
  stav nedokončených kroků (ntfy, Calendar, DB report).
  Hledání: search_files(query="{AgentName}", folderId="{agentTypeFolderId}")
  Zápis: sheets_append_rows(spreadsheetId, range="error", rows=[...])
  Spouští se PŘED finálním reportem operátorovi.
Inputs: AgentName, MCP identifikátor, errors[] s ProblemScore, stav ntfy/Calendar/DB report kroků
Outputs: append do tabu 'error' v master spreadsheet {AgentName}
Limitations: best-effort — Drive nedostupný → přeskočit tiše, capability_missing("problem_report_drive")

=============================================================
PATCH 9.2.0 — SKILLS GOVERNANCE UPDATE
=============================================================
(additive patch — nenahrazuje existující skill definice; opravuje níže uvedená pravidla)

## agent-schedule-manager — 9.2.0 Update
(přepisuje ±1 min conflict window z původní definice)

Conflict detection: zakázána. Paralelní běh více agentů je povolený.
Inputs: AgentName, ScheduledRunTime, IntervalHours, AgentType
Outputs: upsert do AgentSchedules (POUZE při db_reachable), startup report konfliktů
Limitations: vkládat/aktualizovat AgentSchedules POUZE při db_reachable;
  reportovat schedule_drift_detected pokud DB plán ≠ Hermes plán

## problem-report-writer — 10.0.1 Update

Tab 'error' v master spreadsheet {AgentName} (VÝHRADNĚ tab 'error', NIKDY samostatný soubor):
  Formát: ProblemScore | Category | Description | Timestamp | StepStatus
  Master spreadsheet: {AgentName} v Managers složce (ID: 1lEvffJ-rjdExCwMWxmWM7PchnKkGTGUO)
Tab 'error' je součástí master spreadsheet — vytváří se při BOOTSTRAP spolu s ostatními taby.
Pokud master spreadsheet existuje: přečíst tab 'error' při startu a ověřit aktuálnost problémů.
  get_spreadsheet_range(spreadsheetId, range="error!A:Z")
Při selhání: append nový řádek — NIKDY nepřepisovat celý tab.
  sheets_append_rows(spreadsheetId, range="error", rows=[...])
Pokud Drive write při selhání selže: uložit do AgentRunReports.Metadata pokud DB dostupná.
NEVYTVÁŘET samostatný soubor jako náhradu za tab 'error' v master spreadsheet.

## calendar-event-manager — 9.4.2 Update

CALENDAR_ID je pevně nastaven dle AgentType — list_calendars() není potřeba:
  AgentType=Manager: "a43e278e3a817484c52c5fc24439845d6aa230c2280ca25eb2a08bae2e5f4eef@group.calendar.google.com"
NIKDY nepoužívat zobrazovaný název ("AI Managers") jako calendar_id parametr přímo.
Calendar failure = reporting degradation, ne datový blocker.

## Skills Version Policy (9.4.7)

SkillsVersion: 10.0.1
Minimální počet aktivních skills pro plný run: záleží na AgentType (viz PromptFile).
Pokud skills soubor nebyl načten z Drive: capability_missing("skills_file_unavailable") → degraded mode.
